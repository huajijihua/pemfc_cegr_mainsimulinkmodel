# PEMFC-cEGR 项目规则

## 入口与范围

- 默认使用中文沟通。进入项目先读本文件和根目录 `PROJECT.md`；涉及工程边界时再读 `04_Simulink物理网络模型/04_说明/README.md`。历史归档和结果目录只按目标问题读取。
- 本仓只承载两条活动主线：完整系统/面板，以及阴极回路 + 电堆聚焦模型。当前资产、状态、runner、证据边界和下一步以 `PROJECT.md` 为准。
- `00_支撑材料/` 只提供来源；旧标定、10 kW/DQ60/公司临时资料只能作为显式 `external_case`，不能进入 `platform_default`。

## 研究与实现

- 非简单任务先固定：工程决策、系统边界、保真度、输入输出与单位、参数来源、KPI、验证数据、成功/停止条件和允许修改范围。
- 区分来源事实、工程假设、agent 推断、仿真结果和已验证结论；参数标记为直接来源、辨识、迁移或假设。
- 先建立最小可运行基线，再增加耦合、控制、扫参或优化。失败时处理第一条真实阻塞错误，不同时改变物理假设、控制参数和数值设置。
- 每条主线只维护一个正式 runner；工况通过配置或 `Simulink.SimulationInput` 表达，不复制 `.slx` 或 runner。

## MATLAB/Simulink 执行面

- 每个任务首次使用 MATLAB 时，先确认核心 `matlab` MCP 与项目 `satk` MCP 均已暴露。核心 MCP 调用 toolbox 探测并用 `feature('getpid')` 建立专属新会话；随后在该会话执行 `addpath("C:\Users\ADMIN\.matlab\agentic-toolkits\simulink"); satk_initialize; shareMATLABSession`，再通过 `satk` MCP 读取 PID。两侧 PID 必须相同，同一任务 PID 必须稳定；并行任务的 PID 必须不同。启动多个任务时逐个完成双通道绑定后再启动下一个，避免 `existing` 路由抢接最近共享会话。
- 正式 `.slx` 结构读取、修改和检查必须使用 SATK，顺序为 `model_overview/model_read -> model_edit -> model_read -> model_check`。核心 MATLAB MCP 只承担会话握手、正式 runner、仿真、参数研究和必要短探针，不作为结构修改的降级路线；不得用终端 `matlab -batch`、GUI 自动化、解包或文本方式修改结构化模型。
- 核心 MATLAB MCP 或 SATK 未暴露、两通道 PID 不一致、许可证/API 不足，或无法完成结构读回时，停止对应 MATLAB/Simulink 结构任务并报告；可以继续静态研究、方案设计或不涉及模型结构写入的计算，但不得声称已修改或验证模型。
- 不使用 `restoredefaultpath`、`clear all`、`clearvars` 或 `bdclose all`。正式长逻辑写入项目 `.m` 文件，短探测才使用内联执行。

## 验证、结果与文档

- 分别报告：已写入、已读回、已编译/执行、行为验证、工程适用范围内验证。无报错、成功编译或生成图形不等于物理正确。
- 阶段收口前通过 MATLAB 保存正式模型，确认 `Dirty=off`、文件已写盘和提交范围，再完成结构、数值和物理层验证。
- 原始 `.mat`、完整时序、日志、截图、`slprj/` 和 `.slxc` 默认是本地运行产物；Git 只保留复现所需的模型、代码、输入、紧凑 KPI/清单和经明确裁决的里程碑证据。
- 当前状态覆盖写入 `PROJECT.md`，公共工程边界覆盖写入 `04_说明/README.md`。只有确需历史审计时才在统一归档批次增加记录；禁止为普通探测、每次异常或单个工况创建新计划、交接、模型、runner 或过程报告。
- 原始案例和历史证据默认只读。用户未明确要求时不覆盖唯一模型，不删除正式结果；缓存和明确的临时生成物可按 `.gitignore` 处置。

## 可复用工程项目资产治理基线

以下规则可直接迁移到其他技术研究、建模和仿真项目；迁移时只需替换项目边界、正式入口和数量门限。

1. **真源唯一**：项目只维护一个当前状态文件 `PROJECT.md`、一个 agent 入口 `AGENTS.md`，以及必要的工程契约。状态、规划和接口优先覆盖更新，不按日期、阶段或对话复制文档。
2. **职责分层**：每项资产只属于一种角色：活动真源、可复现输入、正式证据、历史归档或可重建生成物。活动任务默认不读取归档、原始结果全集和缓存。
3. **新建有门**：新文件必须承担现有资产无法表达的独立职责。新工况优先增加配置，新算法优先增加函数，新阶段优先更新项目状态；不得用文件副本表达版本、异常或任务进度。
4. **入口受控**：每条研究主线原则上只维护一个正式 runner。模型变化只有改变物理架构、保真度或接口契约时才允许新建正式模型；参数、控制和边界变化通过配置或标准实验输入表达。
5. **结果可追溯**：按“一个研究问题一个运行目录、一个最终摘要”组织。实验至少关联模型/代码版本、配置、求解设置、KPI、状态和异常。原始结果按复现和审计价值决定，不按体积自动删除。
6. **失败不分叉**：失败先定位第一条真实阻塞错误；修复原正式入口，不为每次尝试创建新 runner、模型副本、交接报告或日志说明。
7. **完成即收口**：任务结论进入当前真源、正式代码或紧凑证据后，阶段脚本和过程材料退出活动区。归档按里程碑成批进行，不按单次任务建立归档目录。
8. **读取最小化**：agent 默认只读 `AGENTS.md`、`PROJECT.md`、目标资产及其直接依赖；只有追溯特定结论时才定向读取归档。禁止为了“了解项目”递归加载完整文件树、历史说明或全量时序。
9. **门禁可执行**：数量、位置、命名和生成物规则尽量由只读检查器验证。硬门禁阻止继续膨胀；结果和缓存阈值只触发人工审查，不能代替工程判断。

### 本项目门限

- 活动区只允许根目录 `AGENTS.md`、`PROJECT.md` 和 `04_说明/` 内不超过 5 个工程说明 Markdown。只有拥有独立目标资产、多个实施阶段和验收门的具体工程任务才可维护一份执行计划；普通专题、探测和阶段过程不另建说明文件。
- 完整系统正式 `.slx` 保持 1 个；聚焦模型总数上限 12 个。新架构模型必须对应已裁决的物理边界，并同步更新 `PROJECT.md`。
- 活动 `run_*.m` 总数上限 10 个。正式 runner、测试和可复用库可长期保留；阶段性验证入口在结论并入正式 runner 或紧凑证据后进入统一里程碑归档。
- 原始 MAT 可因复现或审计价值保留；活动结果超过 100 个 MAT 或单目录超过 50 个 MAT 时触发人工留存审查，不自动删除。
- 阶段收口或提交前运行 `04_Simulink物理网络模型/03_脚本/check_project_hygiene.ps1`。硬门禁必须通过；缓存和结果数量告警用于审查，不等同失败。
