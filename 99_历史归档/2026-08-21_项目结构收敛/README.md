# 2026-08-21 项目结构收敛

本批次保存重构前的说明体系和旧汇报资产。

## 已归档

- `04_说明_旧体系/`：原 `04_Simulink物理网络模型/04_说明/` 完整目录，保留原层级和相对关系。
- `05_汇报_旧资产/`：旧 PPT、图、JSON 和生成脚本。
- `03_脚本_阶段性runner/`：9 个已完成阶段任务且不再作为活动入口的验证 runner；历史复现时按需读取。

## 已清理

- 用户已删除本次收敛中隔离的 77 个历史/原始 MAT，约 557.47 MB。
- 用户已删除根目录 `slprj/`、`.satk/` 和隔离的 `.slxc` 等可重建缓存。空的 `.agents/`、`.codex/` 是客户端保留目录，未移动。

紧凑 Markdown/CSV/JSON、工作簿、图片和研究来源保留；项目在操作前已有用户备份。后续有复现或审计价值的原始结果允许保留，不再以体积作为自动清理依据。
