# 历史归档

本目录只保存已退出活动链、但仍有追溯价值且不适合直接删除的文档、汇报和历史资产。

- 归档内容默认只读，不作为模型、参数、runner、验证状态或下一步计划的当前真源。
- 当前工程状态以根目录 `PROJECT.md` 为准。
- 可由正式 runner 重建的原始时序、缓存和临时导出不进入归档，直接删除。
- 需要恢复归档内容时，先在 `PROJECT.md` 中明确恢复目的和资产角色，不直接复制回活动目录。
